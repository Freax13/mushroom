[package]
name = "tests"
version = "0.1.0"
edition.workspace = true

[dependencies]
bytemuck = "1.24.0"
nix = { version = "0.30.1", features = ["event", "fs", "mman", "net", "poll", "signal", "socket", "time", "uio"] }
