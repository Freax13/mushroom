[package]
name = "tests"
version = "0.1.0"
edition.workspace = true

[dependencies]
nix = { version = "0.30.1", features = ["event", "fs", "mman", "mount", "net", "poll", "signal", "socket", "uio"] }
